apiVersion: v1
kind: Service
metadata:
  labels:
    app: backend
  name: backend
  namespace: catcatgo
spec:
  ports:
    - name: http-backend
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: backend
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
